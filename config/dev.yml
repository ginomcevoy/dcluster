---

paths:
  templates: 'templates'
  bootstrap: '$PWD/bootstrap'
  work: $PWD/.dcluster
  ansible_static: 'ansible_static'
  flavors:
    - config/flavors
    - ~/.dcluster/flavors

prefs:
  log_level: DEBUG
